# Core voice agent
livekit-agents>=0.11.0
livekit-plugins-silero>=0.7.0  # VAD
livekit-plugins-openai>=0.10.0  # used for OpenAI/Whisper compat
livekit-plugins-anthropic>=0.2.0
livekit-plugins-elevenlabs>=0.7.0
livekit-plugins-azure>=0.5.0
livekit-plugins-google>=0.7.0

# LLM SDKs
anthropic>=0.40.0
openai>=1.50.0
google-cloud-aiplatform>=1.70.0  # Vertex AI for Gemma

# STT - Hebrew
faster-whisper>=1.0.3
ivrit-ai-stt @ git+https://github.com/ivrit-ai/whisper-runner.git ; python_version >= "3.11"
# (or use ctranslate2 with downloaded ivrit model)

# TTS
elevenlabs>=1.10.0

# RAG / Vector DB
qdrant-client>=1.12.0
cohere>=5.10.0
sentence-transformers>=3.0.0  # fallback embeddings

# Document processing (Hebrew-aware)
pypdf>=5.0.0
pdfplumber>=0.11.0  # better Hebrew PDF extraction
python-docx>=1.1.0
markdown>=3.7
beautifulsoup4>=4.12.0
unstructured[all-docs]>=0.15.0

# Web scraping (for URL ingestion)
httpx>=0.27.0
trafilatura>=1.12.0  # great for article extraction

# Hebrew NLP (optional, for advanced chunking)
# hebrew-tokenizer>=2.3.0

# Phone integration
twilio>=9.3.0

# Backend / API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Utilities
python-dotenv>=1.0.1
structlog>=24.4.0
tenacity>=9.0.0  # retries
sentry-sdk[fastapi]>=2.15.0

# Dev / testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.7.0
mypy>=1.13.0
